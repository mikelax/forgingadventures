server {
    listen 80;
    server_name www.forgingadventures.com;

    location / {
        #api server index.js location
        passenger_app_root /home/app;
        passenger_app_type node;
        passenger_startup_file index.js;

        passenger_enabled on;
        passenger_user app;
      }
}
